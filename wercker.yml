box: php:7.1
build:
  steps:
    - script:
        name: echo PHP information
        code: |-
            php -v
            php -i
    - script:
        name: PHPUnit integration tests
        code: |-
            cd test/UnitTest
            phpunit -c phpunit.xml lib/
